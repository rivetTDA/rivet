#ifndef CONTROLDOT_H
#define CONTROLDOT_H

#include <QGraphicsItem>
#include <QPainter>

class ControlDot : public QGraphicsItem
{
public:

    ControlDot(qreal x, qreal y, qreal width, qreal height, QGraphicsItem* parent = 0);
    
signals:
    
public slots:
    
};

#endif // CONTROLDOT_H
